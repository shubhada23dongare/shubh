void swap(int,int);
int main()
{
   int a=100,b=200;
   cout<<"before swapping:-"<<"a=" <<a<< "b="<<b<<endl;
   
   swap(a,b);
   cout<<"after swapping:-"<<"a="<<a<<"b="<<b;
   
}

void swap(int x,int y)
{
    int z;
    z=x;
    x=y;
    y=z;
  
}
